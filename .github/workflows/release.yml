# ExamGuard Desktop Release Pipeline (Phase 6 & 19)
# Triggers on git tags matching v* (e.g. v0.3.0)
# Builds Windows NSIS installer, macOS DMG, and Linux AppImage via electron-builder

name: Desktop Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  NODE_VERSION: '22'

jobs:
  build-and-release:
    name: Build Desktop Artifact (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            target: win
            artifact: ExamGuard Setup *.exe
          - os: macos-latest
            target: mac
            artifact: ExamGuard-*.dmg
          - os: ubuntu-latest
            target: linux
            artifact: ExamGuard-*.AppImage

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma Client & Build Packages
        run: |
          cd packages/database && npx prisma generate

      - name: Build Electron App (${{ matrix.target }})
        run: pnpm --filter @examguard/student-desktop package:${{ matrix.target }}

      - name: Create GitHub Release & Upload Artifact
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v0.3.0
          files: apps/student-desktop/dist/installer/*
          draft: false
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
